# hands — web project

Minimal project for a small website. Structure:

- `src/` — site source (HTML/CSS/JS)

Suggested local workflow

- Install any dependencies (if used).
- Build or run dev scripts from `package.json` (for example, `npm run dev:css`).
